我将按照以下步骤实现密码显示/隐藏图标的修改：

1. **修改登录页面（LoginPage.kt）**：
   - 导入 `androidx.compose.ui.res.painterResource` 和 `com.gradle.aicodeapp.R`
   - 将密码输入框的 trailingIcon 从锁图标替换为使用 painterResource 根据 passwordVisible 状态显示不同的眼睛图标
   - 使用 `AnimatedVisibility` 添加过渡动画效果

2. **修改注册页面（RegisterPage.kt）**：
   - 同样导入必要的资源
   - 将密码和确认密码输入框的 trailingIcon 替换为眼睛图标
   - 保持与登录页面一致的交互体验

3. **实现的功能特性**：
   - 使用自定义的眼睛图标（睁眼/闭眼）表示密码显示状态
   - 添加适当的过渡动画
   - 确保足够大的点击区域
   - 保持良好的可访问性
   - 与整体UI风格保持一致

4. **技术实现**：
   - 使用 `painterResource` 加载自定义drawable资源
   - 根据 `passwordVisible` 状态切换图标
   - 使用 `AnimatedVisibility` 实现平滑过渡
   - 确保代码简洁可维护

这些修改将提升用户体验，使密码显示/隐藏功能更加直观和易用。